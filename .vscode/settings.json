{
    "makefile.makefilePath": "${workspaceFolder}",
    "makefile.launchConfigurations": [
        {
            "cwd": "${workspaceFolder}/bin",
            "binaryPath": "${workspaceFolder}/bin/main",
            "binaryArgs": []
        }
    ]
}
